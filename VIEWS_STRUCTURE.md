# Views 目录结构说明

## 📁 目录结构概览
- **代码结构由Doubao - Douyin 生成**
- **代码由Lingma - Alibaba Cloud AI Coding Assistant 生成**
- **提示词优化由Doubao - Douyin + 千问 生成**

```
src/views/
├── home/                    # 首页模块
│   ├── HomeIndex.vue       # 首页主页面
│   └── components/         # 首页私有组件（预留）
├── map/                     # 地图模块
│   ├── MapIndex.vue        # 地图功能索引页
│   ├── MapDetail.vue       # 地图详情页
│   └── components/         # 地图私有组件
│       ├── MapControls.vue # 地图控制面板
│       └── MarkersList.vue # 标记点列表组件
├── user/                    # 用户模块
│   ├── UserIndex.vue       # 用户中心首页
│   ├── UserSettings.vue    # 用户设置页面
│   └── components/         # 用户私有组件（预留）
└── error/                   # 错误页面模块
    ├── NotFound.vue        # 404页面
    ├── ServerError.vue     # 500页面
    └── components/         # 错误页私有组件（预留）
```

## 🏗️ 设计原则

### 1. 按业务功能模块划分
- 每个业务领域作为一个独立模块目录
- 模块名称使用小写英文复数形式
- 模块间职责清晰，降低耦合度

### 2. 私有组件隔离
- 每个模块可在其目录下创建 `components` 子目录
- 存放仅该模块使用的私有组件
- 避免全局组件污染，提高组件复用性

### 3. 页面组件命名规范
- 主页面使用 `[ModuleName]Index.vue` 格式
- 详情页面使用 `[ModuleName]Detail.vue` 格式
- 列表页面使用 `[ModuleName]List.vue` 格式
- 表单页面使用 `[ModuleName][Action].vue` 格式

## 📋 各模块说明

### Home 模块 (`/`)
- **主要页面**: `HomeIndex.vue` - 项目首页和入口
- **功能**: 项目介绍、技术栈展示、功能导航
- **路由**: `/`

### Map 模块 (`/map`)
- **主要页面**: 
  - `MapIndex.vue` - 地图功能概览和导航
  - `MapDetail.vue` - 交互式地图详情页
- **私有组件**:
  - `MapControls.vue` - 地图控制面板组件
  - `MarkersList.vue` - 标记点列表组件
- **功能**: 地图展示、标记点管理、底图切换
- **路由**: `/map`, `/map/detail`

### User 模块 (`/user`)
- **主要页面**:
  - `UserIndex.vue` - 用户中心首页
  - `UserSettings.vue` - 用户设置页面
- **功能**: 用户信息展示、账户设置、个人资料管理
- **路由**: `/user`, `/user/settings`

### Error 模块
- **主要页面**:
  - `NotFound.vue` - 404错误页面
  - `ServerError.vue` - 500服务器错误页面
- **功能**: 错误处理、友好提示、用户引导
- **路由**: `*` (404), `/500`

## 🔄 路由映射关系

| 路径 | 组件 | 模块 | 页面标题 |
|------|------|------|----------|
| `/` | HomeIndex | home | 首页 |
| `/map` | MapIndex | map | 地图功能 |
| `/map/detail` | MapDetail | map | 地图详情 |
| `/user` | UserIndex | user | 用户中心 |
| `/user/settings` | UserSettings | user | 账户设置 |
| `*` | NotFound | error | 页面未找到 |
| `/500` | ServerError | error | 服务器错误 |

## 🎯 开发规范

### 新增模块流程
1. 在 `src/views/` 下创建模块目录（小写复数形式）
2. 在模块目录下创建页面组件
3. 如需要私有组件，在模块目录下创建 `components` 子目录
4. 更新路由配置文件 `src/router/index.js`
5. 在导航菜单中添加相应链接

### 组件导入规范
```javascript
// 推荐：按模块组织导入
import MapControls from '@/views/map/components/MapControls.vue'
import MarkersList from '@/views/map/components/MarkersList.vue'

// 路由组件使用动态导入
const MapDetail = () => import('@/views/map/MapDetail.vue')
```

### 命名约定
- **目录名**: 小写英文复数形式（如 `maps`, `users`）
- **组件名**: PascalCase（如 `MapDetail`, `UserSettings`）
- **文件名**: PascalCase（如 `MapDetail.vue`, `UserSettings.vue`）
- **私有组件**: 添加模块前缀避免命名冲突

## 🔧 扩展建议

### 未来可扩展模块
```
src/views/
├── dashboard/    # 仪表板模块
├── reports/      # 报表模块  
├── admin/        # 管理后台模块
├── help/         # 帮助中心模块
└── ...
```

### 组件复用策略
1. **私有组件**: 仅在当前模块内使用
2. **共享组件**: 提升到 `src/components/` 目录
3. **工具组件**: 提升到 `src/components/common/` 目录

这种结构化设计使项目更加清晰易维护，便于团队协作开发。